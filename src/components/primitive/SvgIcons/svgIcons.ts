import { arrowRight, arrowLeft, arrowDown, hamburgerMenu } from '../../../assets';

export const ICONS = {
  arrowDown,
  arrowRight,
  arrowLeft,
  hamburgerMenu,
};

export type IconType = keyof typeof ICONS;
